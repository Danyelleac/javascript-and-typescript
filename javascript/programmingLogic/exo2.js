//Escreva uma função chamada landscapeView que recebe dois argumentos, largura e altura de uma imagem(number).Retorne true se a imagem estiver no modo paisagem.
const landscapeView = (b,h)=>{
    return b>h?true:false
}

//melhorando

const landscapeView1 = (b,h)=>{
    return b>h
}

//melhorando

const landscapeView2 = (b,h)=> b>h